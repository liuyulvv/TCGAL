use crate::kernel::{line_2::Line2, number_type::NumberType, point_2::Point2, ray_2::Ray2};

pub fn line_2_ray_2_intersection<T: NumberType>(
    line: &Line2<T>,
    ray: Ray2<T>,
) -> Option<Point2<T>> {
    todo!()
}
